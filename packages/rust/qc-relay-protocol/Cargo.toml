[package]
name = "qc-relay-protocol"
version = "0.1.0"
publish = false
edition = "2021"
description = "Shared wire frames for QC Remote public-relay device clients"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
