[package]
name = "qc-device-runtime"
version = "0.1.0"
publish = false
edition = "2021"
description = "Platform-neutral Quad Cortex state and operation runtime"

[dependencies]
base64 = "0.22"
prost = "0.14"
qc-protocol = { path = "../qc-protocol" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
