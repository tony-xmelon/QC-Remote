[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "qc-remote-gateway-client"
version = "0.1.0"
description = "Typed Python client for the QC Remote gateway v1 contract"
classifiers = ["Private :: Do Not Upload"]
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/qc_gateway_client"]
