name: Software parity

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      build_distributables:
        description: Build signed private-test or public-release artifacts
        required: true
        default: false
        type: boolean
      distribution_scope:
        description: Intended distribution scope for generated artifacts
        required: true
        default: private-test
        type: choice
        options:
          - private-test
          - public-release

concurrency:
  group: software-parity-${{ github.ref }}
  cancel-in-progress: true

jobs:
  software-parity:
    name: Shared contracts, apps, and native runtimes
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Install Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Restore Rust build cache
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ~/AppData/Local/QCControlBuild/software-parity-target
          key: ${{ runner.os }}-software-parity-rust-v2-${{ hashFiles('**/Cargo.lock') }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-software-parity-rust-v2-${{ hashFiles('**/Cargo.lock') }}-
            ${{ runner.os }}-software-parity-rust-v2-

      - name: Install JavaScript dependencies
        run: npm ci

      - name: Verify shared contracts, apps, and native runtimes
        run: npm run test:parity:software

      - name: Verify pinned pyquadcortex pull request metadata
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run parity:pyquadcortex:github

  ui-conformance:
    name: Responsive and accessible app surfaces
    runs-on: windows-latest
    timeout-minutes: 15

    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Install Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install JavaScript dependencies
        run: npm ci

      - name: Install browser for responsive and accessibility checks
        run: npx playwright install chromium

      - name: Verify responsive layouts and accessibility
        run: npm run test:ui

      - name: Preserve failed UI screenshots and traces
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: ui-conformance-failure-${{ github.run_attempt }}
          path: test-results/
          if-no-files-found: ignore
          retention-days: 14

  android-package:
    name: Android APK and native Rust libraries
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.build_distributables }}
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Install Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install Java 21
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: 21
          cache: gradle

      - name: Install Rust Android targets
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android,x86_64-linux-android

      - name: Restore Rust build cache
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ~/.cargo/bin/cargo-ndk.exe
            packages/rust/*/target
          key: ${{ runner.os }}-android-rust-v2-${{ hashFiles('packages/rust/**/Cargo.lock') }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-android-rust-v2-${{ hashFiles('packages/rust/**/Cargo.lock') }}-
            ${{ runner.os }}-android-rust-v2-

      - name: Install pinned Android build tools
        shell: pwsh
        run: |
          $sdkManager = Get-Command sdkmanager -ErrorAction SilentlyContinue
          if ($null -eq $sdkManager) {
            $sdkRoots = @($env:ANDROID_HOME, $env:ANDROID_SDK_ROOT) | Where-Object { -not [string]::IsNullOrWhiteSpace($_) }
            $sdkManager = $sdkRoots |
              ForEach-Object { Join-Path $_ "cmdline-tools\latest\bin\sdkmanager.bat" } |
              Where-Object { Test-Path -LiteralPath $_ } |
              Select-Object -First 1
          }
          if ($null -eq $sdkManager) { throw "sdkmanager was not found in PATH, ANDROID_HOME, or ANDROID_SDK_ROOT." }
          & $sdkManager "platforms;android-36" "build-tools;36.0.0" "ndk;27.2.12479018"
          if ($LASTEXITCODE -ne 0) { throw "Could not install the pinned Android SDK and NDK tools." }
          if (-not (Get-Command cargo-ndk -ErrorAction SilentlyContinue)) {
            cargo install cargo-ndk --version 4.1.2 --locked
            if ($LASTEXITCODE -ne 0) { throw "Could not install cargo-ndk 4.1.2." }
          }

      - name: Install JavaScript dependencies
        run: npm ci

      - name: Enforce public-release legal gate
        if: ${{ inputs.distribution_scope == 'public-release' }}
        run: npm run legal:check

      - name: Restore registered Android signing identity
        shell: pwsh
        env:
          QC_ANDROID_KEYSTORE_BASE64: ${{ secrets.QC_ANDROID_KEYSTORE_BASE64 }}
        run: |
          if ([string]::IsNullOrWhiteSpace($env:QC_ANDROID_KEYSTORE_BASE64)) { throw "QC_ANDROID_KEYSTORE_BASE64 is required for a hardware-testable Android candidate." }
          $keystore = Join-Path $env:RUNNER_TEMP "qc-control.keystore"
          [System.IO.File]::WriteAllBytes($keystore, [Convert]::FromBase64String($env:QC_ANDROID_KEYSTORE_BASE64))
          "QC_ANDROID_KEYSTORE=$keystore" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Build Android APK with shared Rust runtime
        env:
          QC_ANDROID_STORE_PASSWORD: ${{ secrets.QC_ANDROID_STORE_PASSWORD }}
          QC_ANDROID_KEY_ALIAS: ${{ secrets.QC_ANDROID_KEY_ALIAS }}
          QC_ANDROID_KEY_PASSWORD: ${{ secrets.QC_ANDROID_KEY_PASSWORD }}
        run: npm run android:build:debug

      - name: Preserve APK and release provenance
        uses: actions/upload-artifact@v7
        with:
          name: qc-control-android-${{ github.sha }}
          path: |
            artifacts/android/QC-Remote-Android-*.apk
            artifacts/android/QC-Remote-Android-*.apk.source.json
            artifacts/release-manifest.json
            artifacts/sbom.cdx.json
          if-no-files-found: error
          retention-days: 14

  windows-package:
    name: Windows installer and native Rust broker
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.build_distributables }}
    runs-on: windows-latest
    timeout-minutes: 45

    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Install Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Restore Rust build cache
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ~/AppData/Local/QCControlBuild/cargo-target
          key: ${{ runner.os }}-windows-installer-v2-${{ hashFiles('**/Cargo.lock') }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-windows-installer-v2-${{ hashFiles('**/Cargo.lock') }}-
            ${{ runner.os }}-windows-installer-v2-

      - name: Install JavaScript dependencies
        run: npm ci

      - name: Enforce public-release legal gate
        if: ${{ inputs.distribution_scope == 'public-release' }}
        run: npm run legal:check

      - name: Build Windows installer from the parity-approved commit
        shell: pwsh
        run: ./scripts/build-windows-installer.ps1 -SkipPreflight

      - name: Preserve installer and release provenance
        uses: actions/upload-artifact@v7
        with:
          name: qc-control-windows-${{ github.sha }}
          path: |
            artifacts/windows/QC-Remote-Windows-*.exe
            artifacts/windows/QC-Remote-Windows-*.exe.source.json
            artifacts/release-manifest.json
            artifacts/sbom.cdx.json
          if-no-files-found: error
          retention-days: 14

  release-bundle:
    name: Same-commit Windows and Android release bundle
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.build_distributables }}
    needs: [software-parity, ui-conformance, android-package, windows-package]
    runs-on: windows-latest
    timeout-minutes: 10

    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Install Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Download the parity-approved Android candidate
        uses: actions/download-artifact@v8
        with:
          name: qc-control-android-${{ github.sha }}
          path: artifacts

      - name: Download the parity-approved Windows candidate
        uses: actions/download-artifact@v8
        with:
          name: qc-control-windows-${{ github.sha }}
          path: artifacts

      - name: Regenerate and verify combined release provenance
        shell: pwsh
        run: |
          $windows = @(Get-ChildItem artifacts/windows/QC-Remote-Windows-*.exe -File | ForEach-Object FullName)
          $android = @(Get-ChildItem artifacts/android/QC-Remote-Android-*.apk -File | ForEach-Object FullName)
          if ($windows.Count -ne 1 -or $android.Count -ne 1) { throw "Combined release bundle requires exactly one Windows and one Android candidate." }
          node tools/release-provenance.mjs $windows[0] $android[0]
          node tools/release-candidates.mjs verify

      - name: Preserve the hardware-testable cross-platform bundle
        uses: actions/upload-artifact@v7
        with:
          name: qc-control-release-bundle-${{ github.sha }}
          path: |
            artifacts/windows/QC-Remote-Windows-*.exe
            artifacts/windows/QC-Remote-Windows-*.exe.source.json
            artifacts/android/QC-Remote-Android-*.apk
            artifacts/android/QC-Remote-Android-*.apk.source.json
            artifacts/release-manifest.json
            artifacts/sbom.cdx.json
          if-no-files-found: error
          retention-days: 14
